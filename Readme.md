## Домашка 9 на "5"

Изменила архитектуру `BookController`
По примеру юзера создала `ResponseFactory`, `BookFactory` и `BookService`
Добавила логирование в `BookService`
Продемонстрировала ответы запросов в Postman
Показала, как работает логирование в Graylog
